<!DOCTYPE html>
<html>
<head>
  <title>Detalles del Producto</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Detalles del Producto</h1>
  <div>
    <h2>{{product.name}}</h2>
    <p>Precio: ${{product.price}}</p>
    <p>Categoría: {{product.category}}</p>
    <p>Descripción: {{product.description}}</p>
    <p>Descripción: {{ userDataD.cart}}</p>
    <p>Descripción: {{ product._id}}</p>
  </div>
  <!-- Formulario para agregar al carrito -->
<form action="/api/carts/{{ userDataD.cart }}/products/{{ product._id}}" method="POST">
  <input type="hidden" name="_method" value="POST">
   <input type="hidden" name="cid" value="{{userDataD.cart}}">
  <input type="hidden" name="productID" value="{{product._id}}">
  <input type="hidden" name="quantity" value=1>
    <button type="submit">Agregar al carrito</button>
  </form>
  <a href="/api/products">Volver a la lista de productos</a>
</body>
</html>
